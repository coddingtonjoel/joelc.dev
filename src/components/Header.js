import React from "react";
import SVG from "./SVG";

const Header = () => {
    return (
        <div className="header">
            <div className="header-svg">
                <SVG />
            </div>
        </div>
    );
};

export default Header;
